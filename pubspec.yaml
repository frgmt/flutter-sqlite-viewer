name: sqlite_viewer
description: A Flutter package provides a widget to display the contents of SQLite.
version: 1.0.4
author: FRAGMENT, LLC.<info@fragment.co.jp>
homepage: https://github.com/frgmt

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path: ^1.7.0
  sqflite: ^2.0.0+3
  json_table: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint:
