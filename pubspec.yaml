name: sqlite_viewer
description: A Flutter package provides a widget to display the contents of SQLite.
version: 1.0.5
homepage: https://blog.fragment.co.jp/

sqlite_viewer:
  git:
    url: https://github.com/frgmt/flutter-sqlite-viewer.git

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  json_table: ^2.0.1
  path: ^1.8.0
  sqflite: ^2.0.0+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.5.3
